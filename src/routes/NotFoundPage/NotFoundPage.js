import React from 'react';
import NotFoundMain from '../../components/NotFoundMain/NotFoundMain';

export default class NotFoundPage extends React.Component {
  render() {
    return (
      <NotFoundMain />
    );
  }
}